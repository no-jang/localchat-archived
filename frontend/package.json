{
  "name": "localchat",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "turbo run dev",
    "preview": "turbo run preview",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^1.4.3"
  }
}
