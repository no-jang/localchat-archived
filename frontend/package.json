{
  "name": "localchat",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "^1.4.3"
  }
}