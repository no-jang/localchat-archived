rootProject.name = "localchat"

pluginManagement {
    plugins {
        //id("com.google.devtools.ksp") version "1.6.10-1.0.6"
    }

    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}